### 1. 修改了 Android 端的后端接口地址

我们把 `BlockchainApi.kt` 里的接口地址从：

```kotlin
private const val BASE_URL = "http://10.0.2.2:8080"
```

改成了：

```kotlin
private const val BASE_URL = "http://127.0.0.1:8080"
```

---

### 2. 增加了 adb reverse 端口映射

在 Windows PowerShell 里执行了：

```powershell
E:\Android\Sdk\platform-tools\adb.exe reverse tcp:8080 tcp:8080
```

执行后看到：

```text
host-18 tcp:8080 tcp:8080
```

说明映射成功。

---

### 3. 保留了 Windows 到 Linux 虚拟机的端口转发

Windows 上已经配置了：

```text
Windows 8080 -> Linux 虚拟机 8080
```

也就是：

```text
0.0.0.0:8080 -> 192.168.119.128:8080
```

这个没有问题，所以没有继续改 Linux 或区块链服务。

---

# 为什么要这样修改

## 原因 1：模拟器访问不了 `10.0.2.2`

正常 Android 模拟器里，`10.0.2.2` 代表 Windows 主机。

但我们用的是 **Android Automotive 模拟器**，测试发现它访问：

```text
10.0.2.2
```

时报错：

```text
connect: Network is unreachable
```

说明这个模拟器不能通过 `10.0.2.2` 访问 Windows。

所以继续使用：

```kotlin
http://10.0.2.2:8080
```

App 一定会连接失败。

---

## 原因 2：用 `adb reverse` 可以绕过这个网络问题

执行：

```powershell
adb reverse tcp:8080 tcp:8080
```

之后，Android 模拟器访问自己的：

```text
127.0.0.1:8080
```

会被 adb 转发到 Windows 的：

```text
127.0.0.1:8080
```

而 Windows 的 `127.0.0.1:8080` 又已经转发到了 Linux 虚拟机里的 API 服务。

所以最终链路变成：

```text
Android App
 -> 127.0.0.1:8080
 -> adb reverse
 -> Windows 127.0.0.1:8080
 -> Linux 虚拟机 8080
 -> 区块链 API
```

---

# 最终结论

我们修改的核心就是：

```kotlin
BASE_URL 从 10.0.2.2 改成 127.0.0.1
```

并且每次启动模拟器后执行一次：

```powershell
adb reverse tcp:8080 tcp:8080
```
