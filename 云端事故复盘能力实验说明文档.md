# 溯安行：面向智能汽车事故溯源追责的区块链可信存证系统

## 1. 实验名称
基于关键证据保持与责任一致性的云端事故复盘能力评估实验

---

## 2. 实验目的
本实验用于评估系统中云端事故复盘模块的有效性，重点考察其在接收事故结构化数据后，能否稳定生成内容完整、事实一致且便于理解的事故复盘结果。  

实验重点：
- 云端 AI 是否能够覆盖事故中的关键证据点；
- 云端 AI 生成的主要责任因素是否与人工标注结果一致；
- 云端 AI 是否会出现与输入数据不符的事实错误；
- 结构化输入是否能够提升事故复盘质量；
- 本系统方法是否优于模板生成和通用提示生成方式。

---

## 3. 实验对象与对比方案

### 3.1 被测对象
本项目云端事故复盘模块。  
该模块接收事故事件、责任分析结果、风险因子、环境信息、决策链和遥测摘要等结构化输入，并调用大语言模型生成：
- 事故摘要  
- 根因分析  
- 关键证据点  
- 改进建议  

### 3.2 对比方案

| 组别 | 方法 | 说明 |
| ---- | ---- | ---- |
| A组 | 模板生成 | 根据固定规则拼接事故摘要、责任说明和建议 |
| B组 | 通用大模型生成 | 仅输入简要事故描述和基础状态，不提供结构化分析结果 |
| C组 | 本项目方法 | 输入结构化事故数据，并使用项目内提示模板生成复盘 |

---

## 4. 实验数据设计

### 4.1 样本数量
建议至少准备 **30** 个事故样本；若时间允许，推荐扩展至 **50** 个样本（**本仓库默认评估集为 50**，见 `experiment_samples_realistic_50.json`），以提升结果稳定性。

### 4.2 样本类型

| 事故类型（复盘实验常用六类） | 建议数量（30 规模时） |
| -------- | -------- |
| 驾驶员反应不足 | 5 |
| AEB 触发延迟或未触发 | 5 |
| TTC 过低导致碰撞风险 | 5 |
| 驾驶员接管不足 | 5 |
| 环境因素干扰 | 5 |
| 多因素共同作用 | 5 |

与 **溯安行 / 事故溯源枚举** 对齐时，可再增加 **碰撞（`COLLISION`）**、**自动驾驶故障（`AUTOPILOT_FAULT`）** 两类，以覆盖「已发生碰撞上链存证」与「智驾异常/接管」场景。当前仓库 **50 条** 样本的 **8 类分布与登记表** 见 `experiment_lab/复盘与消融实验记录（工作台模板）.md`。

### 4.3 单个样本字段要求
- 事故编号、事故类型  
- 车辆速度、制动状态、转向状态、TTC、AEB 状态、驾驶员接管情况  
- 环境信息、风险预判结果、严重度预测结果  
- 责任分析结果、决策链摘要、遥测摘要  
- 人工标注的关键证据点、主要责任因素和标准复盘文本  

---

## 5. 人工标注规范

### 5.1 标注人员
建议至少由 3 名人员独立标注：  
- 1 名项目成员  
- 1 名具备车辆或交通背景的成员  
- 1 名未深度参与系统开发的成员  
以降低单一主观偏差。

### 5.2 标注内容

| 标注项 | 要求 |
| ------- | ---- |
| 关键证据点 | 标注对事故复盘和责任判断具有直接价值的事实，如 TTC 过低、AEB 未触发、驾驶员未及时制动等 |
| 主要责任因素 | 从驾驶员因素、系统因素、环境因素、多因素共同作用中选择 |
| 标准复盘文本 | 使用 80 至 150 字描述事故经过、关键原因和结论，作为人工参考答案 |

### 5.3 一致性处理
若 3 名标注人员对同一样本存在分歧，应进行集中复核并形成统一版本，作为最终标准答案。

---

## 6. 实验步骤
1. **样本整理**：从项目已有事故场景中选取样本，按统一字段格式整理原始数据  
2. **人工标注**：标注关键证据点、主要责任因素和标准复盘文本  
3. **三组生成**：针对每个样本分别运行 A、B、C 组方法，保留原始输出  
4. **结果匿名化**：打乱三组结果顺序，隐藏来源信息，形成盲评材料  
5. **人工评分**：依据统一规则完成关键证据命中、责任一致性、事实错误和可读性评分  
6. **结果统计**：汇总各组指标均值，计算方法间差异  
7. **结果复核**：检查异常样本和分歧样本，确认统计过程无遗漏  

---

## 7. 评价指标

### 7.1 关键证据召回率
衡量生成文本是否覆盖人工标注的关键证据点：
```

R_e = N_hit / N_gold

```
- `N_hit`：生成文本中正确命中的关键证据点数量  
- `N_gold`：人工标注的关键证据点总数  

### 7.2 责任因子一致率
衡量模型输出的主要责任因素是否与人工标注一致：
```

A_r = N_correct / N_total

```
- `N_correct`：与人工标注一致的样本数  
- `N_total`：样本总数  

### 7.3 事实错误率
统计生成文本中与输入数据不一致的描述比例：
```

E_f = N_wrong / N_claim

```
- `N_wrong`：错误事实条数  
- `N_claim`：生成文本中的事实陈述总数  

### 7.4 可读性评分

| 评分维度 | 说明 |
| -------- | ---- |
| 表达清晰度 | 语言是否容易理解 |
| 结构完整性 | 是否包含事故经过、原因和结论 |
| 用户友好性 | 普通用户是否能够快速理解 |
| 追责适用性 | 是否适合作为事故说明材料 |

---

## 8. 结果记录模板

| 样本编号 | 方法 | 关键证据召回率 | 责任因子是否一致 | 事实错误数 | 可读性评分 |
| -------- | ---- | -------------- | ---------------- | ---------- | ---------- |
| S01 | A | 0.60 | 否 | 2 | 3.0 |
| S01 | B | 0.80 | 是 | 1 | 4.0 |
| S01 | C | 1.00 | 是 | 0 | 4.7 |

---

## 9. 消融实验设计
为验证端侧结构化分析结果对云端复盘的贡献，可在主实验基础上设置消融实验。

| 消融方案 | 移除内容 | 重点观察指标 | 验证目的 |
| -------- | -------- | ------------ | -------- |
| D1 | 去掉责任分析结果 | 责任因子一致率、可读性评分 | 检验结构化责任分析是否提升复盘判断质量 |
| D2 | 去掉环境信息 | 根因解释质量、事实错误率 | 检验环境上下文是否有助于形成完整解释 |
| D3 | 去掉决策链 | 关键证据召回率 | 检验决策过程是否增强证据覆盖 |
| D4 | 取消结构化提示词，仅保留普通描述 | 事实错误率、关键证据召回率 | 检验结构化提示是否抑制幻觉、提升稳定性 |

> 消融实验应与完整输入条件下的 C 组结果比较，以验证端侧分析、上下文补充和提示组织的作用。

---

## 10. 结果分析要求
- 比较三组方法在关键证据召回率上的差异  
- 判断结构化输入是否提高责任因子一致率  
- 分析本项目方法是否降低事实错误率  
- 比较不同方法的可读性评分  
- 归纳哪些事故类型最容易误判  
- 说明云端 AI 更适合作为辅助解释工具，而非最终责任裁定依据  

---

## 11. 注意事项
- 不得将模型输出直接表述为最终司法责任结论  
- 所有输入数据必须来源明确，避免样本内部矛盾  
- 盲评时必须隐藏方法来源  
- 若模型存在明显幻觉，应保留原始输出，不得人工修饰后再计分  
- 若评审意见差异过大，应记录分歧并重新核对评分标准  
- 样本应覆盖简单和复杂场景，不能只选择容易生成的案例  

---

## 12. 建议归档材料
- 实验样本数据表  
- 人工标注规范表  
- 三组提示词模板  
- 原始生成结果  
- 评分表  
- 统计结果汇总表  

---

## 13. 与本仓库工程衔接的操作要点（便于复现实验）

以下与当前 VehTrust 工程实现一致。填表推荐使用 **`experiment_lab/云端事故复盘实验数据记录表（预填版）.md`**（已预填样本与金标准，并附操作清单）；亦可用《云端事故复盘-实验结果记录（填写模板）.md》或 **`experiment_lab/复盘与消融实验记录（工作台模板）.md`**（精简工作台版）。

### 13.1 环境设置

| 项目 | 建议做法 |
| ---- | -------- |
| 后端 | 在 `backend/` 启动 `uvicorn`（默认端口 **8080**），并在 `backend/.env` 中配置 `OPENAI_API_KEY`；可选 `OPENAI_MODEL`、`OPENAI_BASE_URL`（兼容 OpenAI 协议的网关）。 |
| 解码温度 | 后端对 `/api/accident/analyze` 使用 **temperature=0.2**（与《实验结果记录》「〇、解码参数」一致）。 |
| Android 模拟器 | App 内请求地址为 **`10.0.2.2:8080`**，即模拟器访问本机后端；真机调试时需改为电脑局域网 IP。 |

### 13.2 主实验 A / B / C 与批量生成

| 文档步骤 | 工程中的对应操作 |
| -------- | ------------------ |
| §6 步骤 3「三组生成」 | 在 **溯源详情页**「AI 事故原因分析」区块选择 **A / B / C**；消融主实验时选 **D0**。返回 **事故溯源列表页**，点击 **「批量跑 50 条并保存结果」**，将 `assets/experiment_samples_realistic_50.json` 中 50 条依次 POST 并保存合并 JSON；**A、B、C 各跑一次**得到三份原始结果文件（文件名含组别与消融，如 `cloud_experiment_results_A_D0_*.json`）。 |
| 单条调试 | 仍在详情页点 **「生成 AI 事故分析」**，使用当前选中的组别与消融；请求中附带端侧 **derivedSignals**（反应时间、TTC、AEB 等）及可选严重度摘要。 |

### 13.3 消融实验 D1～D4

| 文档 §9 | 工程中的对应操作 |
| ------- | ------------------ |
| D1～D4 | 详情页选择 **C 组**，再选 **D1 / D2 / D3 / D4**；每次返回列表执行一次 **批量跑 50 条**，保存为独立文件，与 **C + D0** 对比。 |

### 13.4 与《实验结果记录》模板的对应关系

| 模板章节 | 填写内容来源 |
| -------- | ------------ |
| **〇** | 模型名、Git/版本、**temperature=0.2**、**N=50**、评审人数等。 |
| **一** | 样本与 `experiment_samples_realistic_50.json` 中 `eventType`、`scenarioName` 对应；**S01～S50** 与 `eventId` 建议自建对照表；「JSON 归档路径」填该 assets 文件或 `backend` 下同名文件的**路径**，勿贴全文。 |
| **二** | 三人标注后的金标准；**N_gold** 为证据条数。 |
| **三** | 盲评打分；**3.5** 填三份（及消融多份）批量结果文件的**磁盘路径或压缩包名**。 |
| **四** | 各消融条件与完整 C（D0）的对比及归档路径。 |

### 13.5 注意事项（与 §11 一致）

批量结果文件为模型**原始输出**，用于归档与计分；**不得**为抬高分数而人工改写后再统计。盲评材料需**隐去 A/B/C 标签**后再发评委（模板 §3.2）。

### 13.6 仅一人、本机 Android Studio 时的建议流程

1. **同一台电脑**：终端常驻 `uvicorn`；Android Studio 开模拟器跑 App（`10.0.2.2:8080`）。  
2. **主实验**：详情页分别选 **A、B、C**（均 **D0**），每次回列表点 **批量跑 50 条**，把生成的 `cloud_experiment_results_{A|B|C}_D0_*.json` 拷到桌面等归档位置（文件名已含 **组别与消融**，无需再手工改名）。  
3. **表格化**：在 `backend/` 执行  
   `python export_batch_results_to_csv.py 桌面\cloud_experiment_results_A_D0_xxx.json 桌面\A_D0.csv`  
   对 B、C 各做一次，用 **Excel / WPS** 打开 CSV；脚本已留出 **R_e、责任一致、N_wrong、可读性** 等列供你**对照金标准手工填写**（文档要求盲评与人工判据，单人可自评但须在报告里注明「单人评审、非三人盲评」局限）。  
4. **辅助初筛（可选）**：在 `experiment_lab/` 执行 `python score_with_gold_assist.py`（见该目录 `README.md`），可生成 **Re_assist** 粗匹配列，**不可替代**正式 \(R_e\)。  
5. **因子/指标**：\(R_e\)、\(A_r\)、\(E_f\) 依赖「金标准 + 计数规则」，**不能**在无金标准时由程序替你判对错；自动化仅限于：**原始 JSON 归档 + CSV 拉平字段**，减少复制粘贴。

### 13.7 哪些能自动化、哪些不能

| 环节 | 能否自动化 | 说明 |
| ---- | ----------- | ---- |
| 批量调云端、保存 JSON | 能 | App 批量按钮 + 拷文件 |
| JSON → 表格列（摘要、证据列表等） | 能 | `backend/export_batch_results_to_csv.py` |
| 与样本 reasons 的**粗匹配**辅助列（Re_assist） | 能（仅供参考） | `experiment_lab/score_with_gold_assist.py`，须在报告中声明口径与局限 |
| 金标准（证据点、主责任、标准复盘） | 不能 | 必须人写（模板 §二）；单人则自己当标注员并写清版本 |
| \(R_e\)、责任一致、\(E_f\)、可读性打分 | 不能全自动 | 需人对照输入与金标准；若课程允许可用「关键词命中」辅助，但仍需人工复核 |
| 真·盲评（隐去 A/B/C） | 单人难以严格盲评 | 可改用「隔日打乱顺序自评」或请同学只评分数不知组别，并在文档中说明 |

### 13.8 实验工作台（50 条样本 + 辅助脚本）

仓库 **`experiment_lab/`** 目录提供：`README.md`（流程总览）、《**云端事故复盘实验数据记录表（预填版）.md**》（依据说明文档全文生成，**预填样本与金标准**并含「如何做实验」章节）、《复盘与消融实验记录（工作台模板）.md》（精简版）、`score_with_gold_assist.py`（**Re_assist** 粗匹配，不可替代正式 \(R_e\)）、`summarize_batch.py`（单次跑批健康度摘要）、`_gen_data_record_template.py`（从 `experiment_samples_realistic_50.json` **重新生成**预填表）。重新生成样本：`backend/generate_experiment_samples.py`。


